/**
 * Assignment: summarize(arr)
 *
 * Write a function that takes an array of mixed values and returns
 * an object with keys: numbers, strings, booleans — each holding
 * the count of that type in the array.
 *
 * Requirements:
 *   - Use typeof to check each element
 *   - Use a for loop
 *   - Return an object literal
 *
 * Example:
 *   summarize([1, 'hello', true, 42, 'world', false, 7])
 *   // → { numbers: 3, strings: 2, booleans: 2 }
 *
 * Run: npm run assignment
 */

// Write your code below:
