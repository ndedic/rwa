/**
 * Assignment: Same async task, three ways
 *
 * Write a function that returns the value 42 after a 1-second delay.
 * Implement it three ways:
 *
 * 1. withCallback(callback)    — calls callback(42) after 1s
 * 2. withPromise()             — returns a Promise that resolves to 42 after 1s
 * 3. withAsyncAwait()          — async function that awaits the promise and returns 42
 *
 * Then call all three and log "got value: 42" from each.
 *
 * Expected output (after ~1s each):
 *   callback: got value: 42
 *   promise:  got value: 42
 *   async:    got value: 42
 *
 * Run: npm run assignment
 */

// 1. Callback version:


// 2. Promise version:


// 3. Async/await version:


// Call all three:
